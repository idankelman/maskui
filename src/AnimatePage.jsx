

import { motion } from 'framer-motion';

const animations = {
    inital : {},
    animate : {},
    exit : {}
}

const AnimatePage = ({children}) => {
    return (
        <div>

            <motion.div >

            </motion.div>
        </div>
    );
}
export default AnimatePage;